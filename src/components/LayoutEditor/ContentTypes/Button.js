import React from 'react';
import Button from '@material-ui/core/Button';

export default props => {
	return (
		<Button color='primary' size='small' variant='outlined' {...props}>Test</Button>
	);
}
